[build]
packages = ["python311", "python3Packages.pip", "gcc", "libffi", "openssl", "zlib"]

[phases.setup]
commands = ["sudo apt install python3-pip --no-cache-dir -r requirements.txt"]

[phases.start]
commands = ["python3 bexx.py"]





